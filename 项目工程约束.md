# 项目工程约束

## 一、项目背景

这是一个面向单个用户的本地优先职业准备助手，服务对象是一名准备后端开发、全栈开发和 AI Agent 相关实习的计算机专业研究生。

项目目标不是制造新的打卡压力，而是降低开始行动的阻力。产品应通过温和、清晰、可执行的方式，帮助用户记录求职进展、整理学习内容，并完成每天最小的一步行动。

核心功能模块包括：

- 首页仪表盘与每日最小行动
- 求职投递记录
- 面经与复盘记录
- 日记与每日状态记录
- 学习主题与资料链接
- AI 工具快捷入口

这是个人工具，不是多用户 SaaS 产品。

------

## 二、技术架构

项目采用**模块化单体应用（Modular Monolith）**架构。

技术栈：

- Next.js App Router
- TypeScript，并启用严格模式
- Tailwind CSS
- shadcn/ui
- Lucide Icons
- SQLite 本地数据库
- Drizzle ORM 与数据库迁移
- Zod 用于表单和服务端输入校验
- Next.js Server Actions 用于数据写入
- LangGraph JS 仅用于简历提取与优化子流程的服务端节点编排；业务状态仍由 SQLite 持久化

除非任务明确要求，否则不要引入以下内容：

- 独立后端服务
- 微服务架构
- Redis
- 消息队列
- Docker Compose
- 用户登录、注册、权限控制
- 多用户协作
- 外部 AI API
- 网页爬虫
- 云同步
- 后台任务队列

简历闭环例外：按用户明确点击发起的 DeepSeek 请求可以经过本地 LangGraph 简历提取与优化子流程；不增加后台执行、云端追踪或未经用户确认的额外模型调用。

------

## 三、目录职责与边界

```text
src/
├─ app/                # 路由、页面组合、布局
├─ features/           # 按业务模块组织功能
├─ components/
│  ├─ ui/              # 通用基础 UI 组件
│  └─ layout/          # 共享布局组件
├─ db/                 # 数据库连接与数据表定义
├─ config/             # 静态配置，例如 AI 工具链接
├─ lib/                # 与具体业务无关的小型工具函数
└─ types/              # 共享类型定义

scripts/               # 一次性脚本，例如 seed、导入、导出
```

目录规则：

- `src/app` 只负责路由、页面组合和 layout，不堆放复杂业务逻辑。
- `src/features/<功能名>` 存放对应模块的组件、Server Actions、查询逻辑、Zod schema、类型和业务逻辑。
- `src/components/ui` 只放通用基础组件，例如 Button、Dialog、Input、Badge。
- `src/components/layout` 只放 Sidebar、Header、PageContainer 等共享布局组件。
- `src/db` 只放 Drizzle Client、Schema 和数据库相关定义。
- `src/config` 只放稳定配置，例如 AI 工具网址、状态映射、导航项。
- `src/lib` 只放没有业务归属的通用工具函数，不能变成万能杂物目录。
- `scripts` 只允许放一次性脚本，例如初始化数据、导出数据、维护脚本。

严禁把页面组件、数据库查询、Server Actions 或业务逻辑放到 `scripts` 中。

------

## 四、数据设计原则

SQLite 是本项目的唯一数据源。

新增功能时，只建立当前功能真正需要的数据表，不要提前设计大量未来可能使用的表。

求职投递记录至少支持：

- 公司名称
- 岗位名称
- 投递渠道
- 当前状态
- 投递日期
- 备注
- 创建时间
- 更新时间

后续的面经记录可以关联投递记录，但在面经模块尚未实现前，不要提前编写复杂关联逻辑。

所有数据库结构修改必须使用 migration。

不得通过删除数据库、静默重建表或覆盖用户数据的方式解决结构问题。

------

## 五、界面设计原则

产品应呈现为一个安静、清晰、可靠的个人工作台。

设计要求：

- 信息层级清楚。
- 留白充足，不做高密度信息墙。
- 以桌面端体验为主，同时保证基本响应式布局。
- 所有表单控件都有清晰标签。
- 基础交互支持键盘操作。
- 每个空页面都应有明确、温和的空状态提示。
- 文案简短、具体、鼓励行动，但不施压。
- 不使用过多动画。
- 不做强制连续打卡、羞辱性提醒、倒计时压力或重度游戏化。

优先展示少量真正有用的内容卡片，而不是堆叠大量统计图和指标。

------

## 六、代码质量要求

- 默认使用 Server Components。
- 只有确实需要浏览器状态或交互时，才使用 Client Components。
- 所有写操作必须通过 Zod 校验输入。
- 页面文件保持轻量，只负责组合页面。
- 每个业务功能应尽量自包含在对应的 `features` 目录中。
- 不创建泛化的 `services` 大目录。
- 不创建巨大 `utils.ts` 或巨大单文件组件。
- 不重复定义相同类型。
- 没有明确需求时，不新增依赖。
- 重复使用的状态值、配置值应抽成命名常量。
- 只有代码意图不明显时才添加注释。
- 代码应便于一名正在学习全栈开发的计算机研究生理解和维护。

------

## 七、明确不做的内容

除非后续任务明确要求，否则不要实现：

- 登录、注册、账号系统
- 多用户协作
- 社交功能
- 支付功能
- 接入 AI 聊天 API
- 自动抓取、转载或复制第三方面经内容
- 推荐算法
- 复杂日程调度
- 原生移动端应用
- 微服务
- 后台消息队列

对于外部学习资料，只保存以下信息：

- 标题
- 来源链接
- 标签
- 用户备注

不得抓取、复制或重新发布第三方内容。

### 明确允许的外部数据例外

简历模块仅允许以下由用户明确发起或确认的处理：

- 用户发起的本地 PDF 或 JD 图片处理；
- 用户确认后发起的 DeepSeek 请求；
- 手动同步用户指定的唯一飞书来源 URL。

投递模块仅允许用户手动发起以下指定飞书多维表格的只读同步：

- `https://qcn5ycyl8hgi.feishu.cn/base/DEVcbyxZHa0y1Os89MBcr0tNnjd?table=tblSgpBe77jUnejO&view=vewgPKxdon`

该同步不得接受任意来源 URL，不得设置后台或定时抓取，也不得向飞书写入收藏、投递或其他本地状态。

应用启动器仅允许在用户启动应用时，对固定 GitHub 仓库 `brdhub/OwnSpace` 发起只读 Release 元数据检查。发现更高正式版本后，必须先取得用户明确确认，才可下载该 Release 中经过文件名、来源与 SHA-256 校验的更新资源；不得静默强制更新、访问任意仓库、安装草稿或预发布版本。

除上述例外外，仍不得进行任何外部调用、抓取、复制或重新发布第三方内容。

------

## 八、任务完成标准

一个任务只有同时满足以下条件才算完成：

1. 用户可见的目标功能能够正常使用。
2. 代码遵守本文件规定的目录边界。
3. 涉及数据结构时，已同步更新数据库 schema 和 migration。
4. 已处理输入校验、表单错误和空状态。
5. TypeScript 类型检查通过。
6. Lint 检查通过。
7. 未破坏已有功能。
8. 如安装、启动或数据库命令发生变化，已更新 README。
9. 最终回复必须说明：
   - 修改了哪些文件；
   - 采用了哪些关键实现决策；
   - 执行了哪些验证命令；
   - 是否存在仅限于当前任务范围之外的限制。
