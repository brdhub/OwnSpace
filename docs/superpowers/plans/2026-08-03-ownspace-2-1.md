# OwnSpace 2.1 实施计划

1. 为 JD 默认选材回退和 DeepSeek 完整覆盖协议补失败测试。
2. 收紧推荐 prompt、响应校验和重试逻辑，使每个正式条目都有评级。
3. 增加优化 prompt、响应 schema、DeepSeek 调用与字段保真校验测试。
4. 实现优化 Server Actions、查询视图和接受/忽略交互，复用既有建议表，不迁移数据库。
5. 完成品牌、版本号、导航、页面标题与简历工作区紧凑布局。
6. 运行测试、TypeScript、ESLint 和生产构建，按结果修正。
